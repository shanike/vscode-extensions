[
    {
        "key": "ctrl+shift+c",
        "command": "editor.action.insertSnippet",
        "args": {
            "name": "my-console.log"
        },
        "when": "!editorHasSelection && !terminalFocus && (editorLangId==javascript || editorLangId==typescript || editorLangId==javascriptreact || editorLangId==typescriptreact)"
    }
]
