[
    {
        "key": "ctrl+shift+c",
        "command": "editor.action.insertSnippet",
        "args": {
            "name": "my-console.log"
        },
        "when": "!editorHasSelection && editorLangId != 'python' && !terminalFocus"
    },
]